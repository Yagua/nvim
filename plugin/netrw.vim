let g:netrw_browse_split = 0    "where the buffer will be open (previos)
let g:netrw_banner = 0          "disable banner
let g:netrw_winsize = 25        "size of window
let g:netrw_localrmdir='rm -r'  "remove directory command
let g:netrw_liststyle = 0       "style list (like Nvim_tree)
